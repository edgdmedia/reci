# Header Logo Assets

Place exported Figma SVG logos here:

- `reci-logo.svg` (used by header template)

Current header path:
`/wp-content/themes/reci-media-hub/assets/reci-logo.svg`

After adding/replacing the SVG, hard refresh the homepage.
