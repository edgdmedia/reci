{
  "name": "wordpress",
  "version": "1.0.0",
  "description": "This repository is now configured for a PHP template workflow (classic/hybrid), so pages can be built from raw HTML/PHP with minimal transformation from Figma exports.",
  "main": "index.js",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "tailwindcss -i ./src/tailwind.css -o ./assets/css/tailwind.css --watch",
    "build": "tailwindcss -i ./src/tailwind.css -o ./assets/css/tailwind.css --minify"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "autoprefixer": "^10.4.27",
    "postcss": "^8.5.6",
    "tailwindcss": "^3.4.17"
  }
}
